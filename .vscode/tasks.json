{
  // See https://go.microsoft.com/fwlink/?LinkId=733558
  // for the documentation about the tasks.json format
  "version": "2.0.0",
  "tasks": [
    {
      "label": "start:debug",
      "type": "shell",
      "command": "yarn",
      // "args": ["run", "start:debug", "build", "packages/plugin"],
      "args": ["run", "start:debug", "build", "format"],
      "problemMatcher": []
    }
  ]
}
